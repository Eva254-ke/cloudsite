---
// Pricing.astro - Professional pricing section
---

<section class="pricing-section" id="pricing">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">Transparent Pricing For Every Stage</h2>
      <p class="section-subtitle">
        Built for operators who need predictable economics, fast deployment, and room to scale.
      </p>
    </div>

    <div class="pricing-highlights" aria-label="Pricing highlights">
      <span class="highlight-pill">No setup fees</span>
      <span class="highlight-pill">Commission-only model with monthly floor</span>
      <span class="highlight-pill">Scale from 1 site to enterprise network</span>
    </div>

    <div class="pricing-grid">
      <article class="pricing-card">
        <div class="pricing-header">
          <p class="plan-chip">Launch</p>
          <h3 class="plan-name">Starter</h3>
          <p class="plan-tagline">For new deployments</p>
        </div>
        <div class="pricing-price">
          <span class="price-percentage">3%</span>
          <span class="price-label">of monthly sales</span>
          <p class="price-minimum">Minimum: <strong>KES 499 / month</strong></p>
        </div>
        <ul class="plan-features">
          <li><span class="check-icon" aria-hidden="true">✓</span>2 routers</li>
          <li><span class="check-icon" aria-hidden="true">✓</span>500 active users</li>
          <li><span class="check-icon" aria-hidden="true">✓</span>M-Pesa billing</li>
          <li><span class="check-icon" aria-hidden="true">✓</span>Core dashboard</li>
        </ul>
        <a href="/contact" class="btn btn-outline btn-full">Start a Conversation</a>
        <p class="plan-note">Break-even at about KES 16,667 monthly sales</p>
      </article>

      <article class="pricing-card popular">
        <div class="popular-badge">Recommended</div>
        <div class="pricing-header">
          <p class="plan-chip">Scale</p>
          <h3 class="plan-name">Growing</h3>
          <p class="plan-tagline">For scaling operators</p>
        </div>
        <div class="pricing-price">
          <span class="price-percentage">2.5%</span>
          <span class="price-label">of monthly sales</span>
          <p class="price-minimum">Minimum: <strong>KES 1,499 / month</strong></p>
        </div>
        <ul class="plan-features">
          <li><span class="check-icon" aria-hidden="true">✓</span>10 routers</li>
          <li><span class="check-icon" aria-hidden="true">✓</span>Unlimited users</li>
          <li><span class="check-icon" aria-hidden="true">✓</span>Advanced analytics</li>
          <li><span class="check-icon" aria-hidden="true">✓</span>Priority support</li>
        </ul>
        <a href="/contact" class="btn btn-primary btn-full">Choose Growing</a>
        <p class="plan-note">Most selected plan for multi-location hotspot operators</p>
      </article>

      <article class="pricing-card">
        <div class="pricing-header">
          <p class="plan-chip">Enterprise</p>
          <h3 class="plan-name">Enterprise</h3>
          <p class="plan-tagline">For multi-site infrastructure</p>
        </div>
        <div class="pricing-price">
          <span class="price-percentage">2%</span>
          <span class="price-label">of monthly sales</span>
          <p class="price-minimum">Minimum: <strong>KES 4,999 / month</strong></p>
        </div>
        <ul class="plan-features">
          <li><span class="check-icon" aria-hidden="true">✓</span>Unlimited routers</li>
          <li><span class="check-icon" aria-hidden="true">✓</span>White-label portal</li>
          <li><span class="check-icon" aria-hidden="true">✓</span>API access</li>
          <li><span class="check-icon" aria-hidden="true">✓</span>Dedicated support</li>
        </ul>
        <a href="/contact" class="btn btn-outline btn-full">Request Enterprise Plan</a>
        <p class="plan-note">Break-even at about KES 250,000 monthly sales</p>
      </article>
    </div>

  </div>

  <div class="pricing-calculator-edge">
    <div class="pricing-calculator">
      <div class="calculator-header">
        <h3>Estimate Monthly Platform Cost</h3>
        <p>Model your expected sales and instantly see the most cost-effective package.</p>
      </div>
      <div class="calculator-content">
        <div class="calculator-input">
          <label for="sales-input">Projected Monthly Sales (KES)</label>
          <input type="number" id="sales-input" placeholder="50000" min="0" class="calculator-field" inputmode="numeric" />
        </div>

        <div class="calculator-presets" aria-label="Quick sales presets">
          <button type="button" class="preset-btn" data-sales="20000">KES 20K</button>
          <button type="button" class="preset-btn" data-sales="50000">KES 50K</button>
          <button type="button" class="preset-btn" data-sales="100000">KES 100K</button>
          <button type="button" class="preset-btn" data-sales="250000">KES 250K</button>
        </div>

        <div class="calculator-results" id="calculator-results">
          <div class="result-card" data-plan="Starter">
            <span class="result-plan">Starter</span>
            <span class="result-amount" id="starter-cost">KES 499</span>
          </div>
          <div class="result-card" data-plan="Growing">
            <span class="result-plan">Growing</span>
            <span class="result-amount" id="growing-cost">KES 1,499</span>
          </div>
          <div class="result-card" data-plan="Enterprise">
            <span class="result-plan">Enterprise</span>
            <span class="result-amount" id="enterprise-cost">KES 4,999</span>
          </div>
        </div>

        <div class="calculator-recommendation" id="calculator-recommendation" aria-live="polite">
          <p class="recommend-title">Best Plan Right Now: <strong id="recommended-plan">Starter</strong></p>
          <p class="recommend-copy" id="recommended-saving">Most cost-effective at your current projected sales.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // Pricing Calculator Logic
  const salesInput = document.getElementById('sales-input');
  const starterCost = document.getElementById('starter-cost');
  const growingCost = document.getElementById('growing-cost');
  const enterpriseCost = document.getElementById('enterprise-cost');
  const recommendedPlan = document.getElementById('recommended-plan');
  const recommendedSaving = document.getElementById('recommended-saving');
  const resultCards = document.querySelectorAll('.result-card');
  const presetButtons = document.querySelectorAll('.preset-btn');
  
  function calculateCost(sales, rate, minimum) {
    const commission = sales * rate;
    return Math.max(minimum, commission);
  }
  
  function formatCurrency(amount) {
    return 'KES ' + amount.toLocaleString('en-KE', { minimumFractionDigits: 0, maximumFractionDigits: 0 });
  }
  
  function updateCalculator() {
    const sales = parseFloat(salesInput.value) || 0;
    
    // Starter: 3%, min 499
    const starterTotal = calculateCost(sales, 0.03, 499);
    starterCost.textContent = formatCurrency(starterTotal);
    
    // Growing: 2.5%, min 1499
    const growingTotal = calculateCost(sales, 0.025, 1499);
    growingCost.textContent = formatCurrency(growingTotal);
    
    // Enterprise: 2%, min 4999
    const enterpriseTotal = calculateCost(sales, 0.02, 4999);
    enterpriseCost.textContent = formatCurrency(enterpriseTotal);

    const totals = [
      { plan: 'Starter', total: starterTotal },
      { plan: 'Growing', total: growingTotal },
      { plan: 'Enterprise', total: enterpriseTotal }
    ].sort((a, b) => a.total - b.total);

    const best = totals[0];
    const second = totals[1];
    const savings = Math.max(0, second.total - best.total);

    if (recommendedPlan) {
      recommendedPlan.textContent = best.plan;
    }

    if (recommendedSaving) {
      if (savings > 0) {
        recommendedSaving.textContent = `Saves about ${formatCurrency(savings)} per month compared to ${second.plan} at this sales level.`;
      } else {
        recommendedSaving.textContent = 'All plans currently cost the same at this sales level.';
      }
    }

    resultCards.forEach((card) => {
      const isBest = card.getAttribute('data-plan') === best.plan;
      card.classList.toggle('result-best', isBest);
    });
  }
  
  if (salesInput) {
    salesInput.addEventListener('input', updateCalculator);
  }

  presetButtons.forEach((btn) => {
    btn.addEventListener('click', () => {
      const value = btn.getAttribute('data-sales');
      if (!salesInput || !value) return;
      salesInput.value = value;
      updateCalculator();
    });
  });

  updateCalculator();
</script>

<style>
  /* Pricing layout */
  .pricing-section {
    padding: 4.5rem 0;
    background: var(--color-gray-50);
    position: relative;
    overflow: hidden;
  }

  .pricing-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 10% 0%, rgba(59, 130, 246, 0.08), transparent 45%);
    pointer-events: none;
  }

  .section-header {
    text-align: center;
    max-width: 760px;
    margin: 0 auto 2.5rem;
  }

  .section-title {
    margin-bottom: 0.75rem;
  }

  .section-subtitle {
    font-size: 1rem;
    color: var(--color-gray-600);
    margin-bottom: 0;
  }

  .pricing-highlights {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
    justify-content: center;
    margin-bottom: 1rem;
    position: relative;
    z-index: 1;
  }

  .highlight-pill {
    display: inline-flex;
    align-items: center;
    padding: 0.34rem 0.65rem;
    border-radius: 999px;
    border: 1px solid var(--color-gray-300);
    background: white;
    color: var(--color-gray-700);
    font-size: 0.76rem;
    font-weight: 600;
  }

  /* Pricing grid */
  .pricing-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
    margin-bottom: 2rem;
    align-items: stretch;
    position: relative;
    z-index: 1;
  }

  /* Pricing card */
  .pricing-card {
    background: white;
    border: 1px solid var(--color-gray-200);
    border-radius: var(--border-radius-lg);
    padding: 1.25rem;
    position: relative;
    transition: var(--transition);
    display: flex;
    flex-direction: column;
    min-height: 100%;
    backdrop-filter: blur(2px);
  }

  .pricing-card:hover {
    border-color: var(--color-primary-light);
    box-shadow: var(--shadow-md);
    transform: translateY(-2px);
  }

  .pricing-card.popular {
    border-color: var(--color-primary);
    box-shadow: var(--shadow-md);
  }

  .popular-badge {
    position: absolute;
    top: -11px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--color-primary);
    color: white;
    padding: 0.3rem 0.85rem;
    border-radius: 2rem;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.2px;
    text-transform: uppercase;
  }

  .pricing-header {
    margin-bottom: 0.75rem;
    padding-bottom: 0.65rem;
    border-bottom: 1px solid var(--color-gray-200);
  }

  .plan-chip {
    margin: 0 0 0.35rem;
    color: var(--color-primary);
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-weight: 700;
  }

  .plan-name {
    font-size: 1.15rem;
    margin-bottom: 0.25rem;
  }

  .plan-tagline {
    font-size: 0.86rem;
    color: var(--color-gray-600);
    margin: 0;
  }

  .pricing-price {
    margin-bottom: 0.8rem;
  }

  .price-percentage {
    display: block;
    font-size: 1.9rem;
    font-weight: 800;
    color: var(--color-primary);
    line-height: 1;
  }

  .price-label {
    display: block;
    font-size: 0.85rem;
    color: var(--color-gray-600);
    margin-top: 0.25rem;
  }

  .price-minimum {
    margin-top: 0.5rem;
    padding: 0.45rem 0.6rem;
    background: var(--color-gray-50);
    border-radius: var(--border-radius);
    font-size: 0.82rem;
    color: var(--color-gray-700);
    margin-bottom: 0;
  }

  .price-minimum strong {
    color: var(--color-primary);
  }

  .plan-features {
    list-style: none;
    padding: 0;
    margin: 0 0 0.8rem;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    flex-grow: 1;
  }

  .plan-features li {
    display: flex;
    align-items: flex-start;
    gap: 0.55rem;
    font-size: 0.82rem;
    color: var(--color-gray-700);
    line-height: 1.45;
  }

  .check-icon {
    width: 18px;
    height: 18px;
    background: var(--color-accent);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.64rem;
    font-weight: 700;
    flex-shrink: 0;
    margin-top: 1px;
  }

  .plan-note {
    font-size: 0.74rem;
    color: var(--color-gray-500);
    margin-top: 0.7rem;
    margin-bottom: 0;
    text-align: center;
  }

  /* Pricing calculator */
  .pricing-calculator {
    background: linear-gradient(135deg, #ffffff 0%, #f8fbff 100%);
    border: 0;
    border-radius: 0;
    padding: 1.2rem 1rem;
    position: relative;
    z-index: 1;
  }

  .pricing-calculator-edge {
    width: 100%;
    margin-top: 1.5rem;
    background: linear-gradient(180deg, rgba(255, 255, 255, 0) 0%, rgba(239, 246, 255, 0.85) 100%);
    border-top: 1px solid var(--color-gray-200);
    border-bottom: 1px solid var(--color-gray-200);
    position: relative;
    z-index: 1;
  }

  .calculator-header {
    text-align: center;
    margin-bottom: 1rem;
  }

  .calculator-header h3 {
    margin-bottom: 0.35rem;
    font-size: 1.2rem;
  }

  .calculator-header p {
    color: var(--color-gray-600);
    margin-bottom: 0;
    font-size: 0.92rem;
  }

  .calculator-content {
    width: 100%;
    max-width: none;
  }

  .calculator-input {
    margin-bottom: 1rem;
  }

  .calculator-presets {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }

  .preset-btn {
    border: 1px solid var(--color-gray-300);
    background: white;
    color: var(--color-gray-700);
    border-radius: 999px;
    padding: 0.35rem 0.7rem;
    font-size: 0.8rem;
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
  }

  .preset-btn:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
  }

  .calculator-input label {
    display: block;
    font-weight: 500;
    margin-bottom: 0.4rem;
    color: var(--color-gray-700);
    font-size: 0.9rem;
  }

  .calculator-field {
    width: 100%;
    padding: 0.85rem 1rem;
    font-size: 1rem;
    border: 1px solid var(--color-gray-300);
    border-radius: var(--border-radius);
    transition: var(--transition);
  }

  .calculator-field:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
  }

  .calculator-results {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.65rem;
  }

  .result-card {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.75rem 0.85rem;
    background: var(--color-gray-50);
    border-radius: var(--border-radius);
    border-left: 3px solid var(--color-gray-300);
    transition: var(--transition);
  }

  .result-best {
    border-left-color: var(--color-accent) !important;
    background: #ecfdf5;
    box-shadow: var(--shadow-sm);
  }

  .result-card:nth-child(1) {
    border-left-color: var(--color-primary);
  }

  .result-card:nth-child(2) {
    border-left-color: var(--color-secondary);
  }

  .result-card:nth-child(3) {
    border-left-color: var(--color-accent);
  }

  .result-plan {
    font-weight: 600;
    color: var(--color-gray-700);
    font-size: 0.9rem;
  }

  .result-amount {
    font-weight: 700;
    font-size: 1.02rem;
    color: var(--color-primary);
  }

  .calculator-recommendation {
    margin-top: 0.9rem;
    padding: 0.85rem;
    border-radius: var(--border-radius);
    background: var(--color-gray-50);
    border: 1px solid var(--color-gray-200);
  }

  .recommend-title {
    margin: 0 0 0.25rem;
    font-size: 0.9rem;
    color: var(--color-gray-800);
  }

  .recommend-copy {
    margin: 0;
    font-size: 0.84rem;
    color: var(--color-gray-600);
  }

  /* Responsive */
  @media (min-width: 768px) {
    .pricing-highlights {
      margin-bottom: 1.4rem;
    }

    .pricing-grid {
      grid-template-columns: repeat(3, 1fr);
    }
    
    .calculator-results {
      grid-template-columns: repeat(3, 1fr);
    }

    .pricing-calculator {
      padding: 1.7rem 1.5rem;
    }

    .calculator-content {
      max-width: 1100px;
      margin: 0 auto;
    }
    
  }

  @media (min-width: 980px) {
    .plan-features {
      display: grid;
      grid-template-columns: 1fr 1fr;
      column-gap: 0.7rem;
      row-gap: 0.45rem;
    }
  }

  @media (min-width: 1024px) {
    .pricing-grid {
      gap: 1.4rem;
    }
  }

  @media (max-width: 767px) {
    .pricing-section {
      padding: 3rem 0;
    }

    .pricing-highlights {
      justify-content: flex-start;
    }
    
    .pricing-card.popular {
      transform: none;
    }
    
    .pricing-card {
      padding: 1rem;
    }

    .pricing-calculator-edge {
      margin-top: 1rem;
    }
  }
</style>